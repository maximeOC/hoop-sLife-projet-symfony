{% extends 'base.html.twig' %}

{% block title %}Produits détaillés{% endblock %}

{% block body %}
    <section>
    <div class="container">
        <div class="product-header">
            <h1> Tous les produits </h1>
            {% if is_granted ("ROLE_SUPER_ADMIN") %}
                <a href="{{ path('admin_products_add') }}" class="btn btn-primary">Ajout</a>
            {% endif %}
        </div>
        <div class="l-container__cardProduct">
            {% for product in products %}
                <div class="b-game-cardProduct">
                    <div class="b-game-cardProduct__coverProduct ">
                        {% for image in product.images %}
                            <img class="product-images" src="{{ asset('build/images/products/') ~ image.name}}" alt="{{ image.alt }}">
                        {% endfor %}
                        <br>
                        <p>{{ product.name }}</p>
                        <p> prix: {{ product.price / 100 }} euros</p>
                        {% if is_granted ("ROLE_SUPER_ADMIN") %}
                            <a href="{{ path('admin_products_edit', {id: product.id}) }}">Modifier</a>
{#                            <a href="{{ path('admin_products_delete') }}"></a>#}
                        {% endif %}
                        <div class="showproduct">
                            <a href="{{ path('products_details', {slug: product.slug}) }}">Voir le produit </a>
                            {% if app.user %}
                                <a href="{{ path('cart_add', {id: product.id}) }}" class="btn-add"><i class="fa-solid fa-cart-shopping"></i> </a>
                            {% else %}
                                <a href="{{ path('app_register') }}" class="btn-add"><i class="fa-solid fa-cart-shopping"></i> </a>
                            {% endif %}
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
    </section>
{% endblock %}
