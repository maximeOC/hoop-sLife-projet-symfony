{{ form_start(formPrice) }}
<h4> Prix</h4>
<div class="row">
    <div class="col-md-6">
        {{ form_row(formPrice.min) }}
    </div>
    <div class="col-md-6">
        {{ form_row(formPrice.max) }}
    </div>
</div>
<br>
<div id="price-slider"></div>
<br>
<button type="submit" class="w-100 filter-button">Filtrer le prix </button>
{{ form_end(formPrice) }}