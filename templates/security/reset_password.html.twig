{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <h1>reinitialisation de mot de passe</h1>
    <div class="container">
        {{ form_start(newPassword) }}
        {{ form_row(newPassword) }}
        <button type="submit"> Envoyer </button>
        {{ form_end(newPassword) }}
    </div>
{% endblock %}



